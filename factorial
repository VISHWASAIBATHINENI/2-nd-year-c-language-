#include <stdio.h>
int main()
{
    int a;
    printf("Enter any number: ");
    scanf("%d", &a);
}
int fact(int);
int fact(int a)
{
    if (a == 1)
        return 1;
    else
        return a * fact(a - 1);
}
